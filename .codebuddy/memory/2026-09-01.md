# 2026-09-01

## vue.ftl → 按 Home/Passenger.vue 的结构重写（setup 语法 + TS）
模板结构现在完全对齐 Passenger.vue：`<template>` → `<script lang="ts" setup>` → `<style></style>`。

关键改动：
- **去掉裸 axios**，改用 `@/api` 封装函数。函数名由 `${Domain}` 推导（与 `web/src/api/index.ts` 实际命名一致）：
  - 查询 `get${Domain}getList`（注意是 `getPassengergetList`，getList 首字母小写，原项目就这么写的）
  - 保存 `save${Domain}`、删除 `delete${Domain}`
- 类型从 `@/api/type` 导入 `${Domain}VO`、`${Domain}QueryResp`（后者正是后端 `queryResp.ftl` 生成的类名，天然对应）。
- 表单从 `ref` 改为 `reactive` + `${domain?upper_case}_INITIAL` 常量 + `Object.assign` 重置（`reset${Domain}()`）。
- 新增 `formRef` + `:rules` 校验，`handleOk` 先 `validate()` 再提交，失败不关闭弹窗。
- 新增 `nextTick(() => formRef.value?.clearValidate())` 清除残留校验红字。
- `v-model:visible`（ant-design-vue 4.x 已废弃）改为 **`v-model:open`**。
- notification 统一带 `message: ""`（ant-design-vue 4 要求）。
- 删除前判空 `if (record.id == null) return;`，保存提示按 `${domain}.id` 区分"编辑成功/新增成功"。

### form / columns 的字段过滤规则（重要）
`id`、`memberId`、`createTime`、`updateTime` **都不出现在表单和表格列中**（id 雪花生成、memberId 登录上下文填充、时间戳后端维护），但 `_INITIAL` 里要保留全部字段。
- Passenger.vue 里也是这个规则，一开始漏了 memberId 会多出一个可编辑的会员id 输入框。

## 踩坑：Vue 模板里不能用 window
- 在 `<template>` 里写 `v-for="item in (window as any).XXX_ARRAY"` 会报
  `TS2339: Property 'window' does not exist on type '{...}'`（模板标识符走组件实例解析，不是全局作用域）。
- 修法：script 里定义顶层常量 `const XXX_ARRAY = (window as any).XXX_ARRAY as {...}[];`，模板里直接引用常量名。`<script setup>` 顶层常量自动暴露给模板。
- 这正是 Passenger.vue 用 `typeMap` 常量的原因。

## 踩坑：tsconfig.app.json 的 baseUrl 导致 vue-tsc 静默不检查【已修复 2026-09-01】
- `web/tsconfig.app.json` 里 `"baseUrl": "."` + `typescript ~6.0.2` → 报
  `TS5101: Option 'baseUrl' is deprecated ...`，**vue-tsc 直接 exit 2 退出，不做任何类型检查**（`pnpm build` 失败）。
- **已修复**：删掉 `baseUrl`，`paths` 改为 `"@/*": ["./src/*"]`（TS 4.4+ 的 paths 相对 tsconfig 自身解析，不需要 baseUrl）。
- 验证：`pnpm build` → exit 0，`vue-tsc -b` 通过 + vite build 产出 dist；`--listFiles` 确认 `@/api/index.ts`、`@/api/type.ts` 等别名文件都被检查到。
- 教训：**"vue-tsc 没报错"可能是假象**。若 TS5101 之类的致命配置错误存在，它会直接退出而不检查。判断检查是否真跑了，要注入故意的错误（如 `const __probe: number = "not-a-number";`）确认能被检出（TS2322 + TS6133）。

## 踩坑：Sider.vue 菜单的 openKeys 残留
- `openKeys = ref(["sub1"])` 但菜单里只有两个 `a-menu-item`，没有 key="sub1" 的 sub-menu（教程代码遗留）。已改为 `ref([])`。
- 注：这不是下面那条 Vue 警告的成因，只是顺带清理。

## 已知无害的 dev 警告：`Slot "default" invoked outside of the render function`
- 现象：`pnpm dev` 时控制台报 `<ATooltip title=null open=false placement="right">` + `at <AMenuItem>`。
- 根因：`ant-design-vue/es/menu/src/MenuItem.js` **无条件**用 Tooltip 包裹内容（第 197 行），非折叠态又把 `title` 置 null、`open` 置 false（第 185-190 行）——正好对应栈里的 `title=null open=false`。
- **不是用户代码问题**（Sider.vue 里没有 a-tooltip），也无法用户侧修复（没有开关，4.2.6 已是 4.x 最新版）。
- **仅 dev 环境**：Vue esm-bundler 里该 warn 被 `process.env.NODE_ENV !== "production"` 包裹。
  **已实测**：`dist/assets/*.js` 中用 .NET 直接读全文本，目标字符串 `invoked outside of the render function` 不存在，而对照组 `Vue`/`function` 存在 —— 确认已被 tree-shaking 移除。
- 结论：可忽略。生产控制台不会出现，不影响功能（菜单是静态内容，无响应式依赖需追踪）。

## ⚠️ 操作教训：验证模板时误覆盖了用户源文件
- 在验证 `vue.ftl` 渲染结果时，`web/src/views/Home/Passenger.vue` 被生成产物覆盖（62 insertions / 37 deletions），已从 git 恢复（`git checkout --`），`git diff` 为空确认还原。
- **教训：模板验证的渲染结果必须写到新文件名（如 `XxxGen.vue`），绝不能覆盖已有源文件。** 验证完立即清理临时文件（含 `target/classes` 里残留的 .class）。
- 判断文件是否真被改动：`git hash-object --no-filters <file>` vs `git rev-parse HEAD:<file>`。
  **注意**：Windows 下若开了 autocrlf，这两个哈希会永远不同（工作区 CRLF vs git 内 LF），此时应以 `git diff --stat` 为空 为准。

## 验证方法（模板改动通用）
1. 临时在 `generate/src/main/java/com/junhao/generate/util/` 写带 `main` 的类，构造 fieldList/param 渲染模板，**用 Java 直接写文件（UTF-8）**。
   - 不要用 PowerShell `>` 重定向：它写 UTF-16LE，会让输出多出伪空行、干扰判断。
   - 运行 classpath：`generate\target\classes` + freemarker + hutool + slf4j-api（+ dom4j/jaxen/mysql-connector-j 用于 ServerGenerate）。
2. 把渲染结果复制成 `web/src/views/.../XxxGen.vue`，跑
   `cd web; npx vue-tsc --noEmit -p tsconfig.app.json --ignoreDeprecations 6.0`（exit 0 即通过）。
3. 清理：删临时 .vue、.txt、临时 java 源文件，以及 `target/classes` 里残留的 .class。

## 【已修】生成的前端页面运行时报 `Cannot read properties of undefined (reading 'reduce')`
- 根因：模板沿用了"后端枚举注入 window"的假设（`window.XXX_ARRAY`），但**本项目没有这套注入机制**（index.html 和 src 下都没有），所以该常量是 undefined，调 `.reduce()` 崩溃。
- 修法：**新增 `EnumUtil`，直接解析后端 Java 枚举类，把真实 code/desc 生成进前端常量**，不再依赖 window。
  - `Field` 新增 `enumName`（枚举类名）+ `enumList`（`[{key:"1",value:"成人"}]`）。
  - `EnumUtil.getEnumName(comment)`：正则 `枚举\[(\w+)]` 从字段注释提取枚举类名。
  - `EnumUtil.parseEnum(module, enumName)`：读 `${module}/src/main/java/com/junhao/${module}/enums/${enumName}.java`，
    用正则 `(\w+)\s*\(\s*"([^"]*)"\s*,\s*"([^"]*)"\s*\)` 解析出 code/desc。文件不存在时返回空列表（不崩）。
  - `ServerGenerate.fillEnumList(fieldList, module)` 在取完字段后调用。
- 代价：枚举变更后需重新生成前端文件（已在生成代码注释中说明）。

## 【已修】vue 文件名大小写导致静默覆盖
- `genVue(do_main, ...)` 生成 `passenger.vue`（kebab 小写），但项目里是 `Passenger.vue`。
  **Windows 文件系统不区分大小写 → 静默覆盖已有大驼峰文件**。
- 修法：改用 `genVue(Domain, ...)`，文件名与后端类名一致（`Passenger.vue`）。

## FreeMarker 陷阱：行尾 FTL 注释会吃掉换行
- 写法 `// 注释文本 ${var}<#-- -->` 会导致下一行代码被拼接到注释里，例如生成
  `// ...枚举类 PassengerTypeEnumconst PASSENGER_TYPE_ARRAY = [` —— `const` 被注释掉，运行时报 not defined。
- 原因：行尾的 `<#-- -->` 会把其后的换行符一并剥离。
- 修法：别在行尾放 FTL 注释；注释写在独立行（整行只含 FTL 标签的行会被 FreeMarker 自动剥离，正是期望行为）。

## 【最终方案】前端枚举改为从 `src/assets/js/enums.ts` 集中引入
- 用户新增了枚举文件，模板改为 **import 而非内联生成/读 window**：
  ```ts
  import { PASSENGER_TYPE_ARRAY } from "@/assets/js/enums";
  const PASSENGER_TYPE_MAP = PASSENGER_TYPE_ARRAY.reduce((map, item) => {
    map[item.code] = item.desc; return map;
  }, {} as Record<string, string>);
  ```
- 枚举字段名统一为 **`code` / `desc`**（与后端 Java 枚举类字段一致；之前内联方案用的 key/value 已废弃）。
- 模板里按需 import：先用 `<#assign enumFields>` 扫 fieldList，无枚举字段则整行不生成（否则触发 `noUnusedLocals` 报错）。
- **重要：文件必须是 `.ts` 不能是 `.js`** —— `tsconfig.app.json` 的 `include` 只有 `src/**/*.ts,tsx,vue` 且 `allowJs` 未开，
  import `.js` 会报 `TS7016: Could not find a declaration file ... implicitly has an 'any' type`。已把 `enums.js` 改名为 `enums.ts`。
- **必须写 `export`** —— 原文件是隐式全局 `PASSENGER_TYPE_ARRAY = [...];`，ESM 严格模式下会 `ReferenceError`。
- 因此 `EnumUtil` / `Field.enumList` / `Field.enumName` 已不再被模板使用（保留代码，可考虑后续清理）。

## 【已修】改文件扩展名后 dev 报 `does not provide an export named 'XXX'`
- 现象：`enums.js` 改名成 `enums.ts` 后，`pnpm dev` 仍请求 `/src/assets/js/enums.js?t=...` 并报该 export 不存在。
- 根因：**改名后残留了一个 0 字节的 `enums.js`**（HMR/编辑过程重建的空壳）。Vite 解析无扩展名的
  `import ... from "@/assets/js/enums"` 时按扩展名优先级 **先命中 `.js`**，空文件自然没有导出。
  （报错 URL 里的 `?t=` 是 Vite 的 HMR cache-busting 参数，不代表缓存问题。）
- 修法：删掉残留的空 `enums.js` + 清 `node_modules/.vite` 缓存 + 重启 dev server。
- 教训：**改扩展名后务必确认旧文件已消失**（`Get-ChildItem -Force`），同名不同扩展名的空壳极难察觉。
- **空壳的来源不是 ServerGenerate**（已两次实测：干净状态下运行，运行前后目录都只有 enums.ts，不会创建 .js）。
  真实来源是 **IDEA 里仍打开着 `enums.js` 的编辑器标签**，用户一保存就把空内容写回磁盘。
  删除后观察 12 秒不会自动重建，也排除脚本自动生成。
- **【最终修法】import 显式写 `.ts` 后缀**（根治，不依赖用户手动删文件）：
  ```ts
  import { PASSENGER_TYPE_ARRAY } from "@/assets/js/enums.ts";
  ```
  这样即便同目录残留同名 `.js`，Vite 也不会去解析它。
  - 可行性依据：`@vue/tsconfig@0.9.1` 基线已开 `allowImportingTsExtensions: true` + `noEmit: true` + `moduleResolution: bundler`，
    无需再改本项目 tsconfig。
  - **已带干扰验证**：故意放一个空 `enums.js` 在目录里，`vue-tsc` 和 `pnpm build` 均通过，
    产物中枚举数据正常且不含 `enums.js` 引用。
- 用户确认要 `.ts`（不是 `.js`）。备选的 `allowJs` 方案已回滚，未采用。

## 【已清理】EnumUtil 及其相关字段（2026-09-01 末）
- 前端枚举改为 import `@/assets/js/enums` 后，`EnumUtil` 整体无用，已删除：
  - 删 `EnumUtil.java`（含 target/classes 里的 .class）
  - `ServerGenerate`：删 import、`fillEnumList()` 方法及其调用
  - `Field`：删 `enumName`、`enumList` 字段和 `java.util.List/Map` import
- 同时清理：临时校验类 `VueTplCheck.java`、冗余备份 `Passenger.vue.orig.bak`。
- 清理后重新生成 + `vue-tsc` + `pnpm build` 均通过。
- 代价：枚举不再自动从后端 Java 类同步，新增/修改枚举需手动更新 `enums.ts`（这是用户选择的方案）。

## 【重大】`enums.js` 反复出现的真凶 = 用户新建的 `EnumGenerator.java`
- 用户新建了 `generate/.../server/EnumGenerator.java`（反射读 `PassengerTypeEnum`，输出前端枚举文件）。
  **它写出的路径就是 `web/src/assets/js/enums.js`**，且格式是**无 export 的隐式全局赋值**：
  ```
  PASSENGER_TYPE_ARRAY=[{code:"1", desc:"成人"},...];
  ```
  这才是 `does not provide an export named 'PASSENGER_TYPE_ARRAY'` 的真正根源。
  （先前判断的"IDEA 编辑器标签保存出空文件"只是表象之一：生成器运行前被 IDE 保存为空，运行后写入无 export 内容。）
- 前几轮排查之所以没发现：它是 untracked 新文件，且我当时只测了 `ServerGenerate`（确实不碰该文件）。

## 【已修】Spring Boot repackage 导致 member 无法被其它模块依赖
- `member` 用 `spring-boot-maven-plugin`，install 后的 jar 是 fat jar，类在 `BOOT-INF/classes/` 下，
  `generate` 依赖它会报 `程序包 com.junhao.member.enums 不存在`（编译期看不到类）。
- **修法（Spring Boot 官方推荐）**：给插件加 `<classifier>exec</classifier>`。
  则 `member-0.0.1-SNAPSHOT.jar` 保持普通 jar（可被依赖），fat jar 变成 `member-0.0.1-SNAPSHOT-exec.jar`。
- ⚠️ **副作用：启动 member 服务须改用 `-exec.jar`**（如 `java -jar member/target/member-0.0.1-SNAPSHOT-exec.jar`）。
  项目里目前只有 `run_gw.bat`（gateway），没有 member 启动脚本，影响可控，但需告知用户。
- 另：解析 member.pom 需要根 pom `train` 也在本地仓库 → 用 `mvn -N install` 装根 pom。
  否则会 warn "The POM for com.junhao:member is invalid, transitive dependencies will not be available"。

## EnumGenerator 警告清零（12 条 → 0）
- `printStackTrace()` ×3 → `LOG.error(...)`（SLF4J，字段名 LOG，与项目一致）
- 原始类型 `Class` → `Class<? extends Enum<?>>`（连带消除 3 条 getMethod 未检查调用警告）
- `StringBuffer` → `StringBuilder`
- `new FileOutputStream` + 手工 close → try-with-resources（消除"可用自动资源管理"和"close 可能 NPE"）
- `OutputStreamWriter(..., "UTF-8")` → `StandardCharsets.UTF_8`
- 补 Javadoc `@param` 说明
- "形参 clazz 的值始终为 PassengerTypeEnum.class"：改为 `ENUM_CLASSES` 列表遍历（新增枚举在列表登记即可，天然消除该警告）
- 运行验证：`PASSENGER_TYPE_ARRAY=[{code:"1", desc:"成人"},...]` 正常生成

## 【已改造】EnumGenerator → 输出 enums.ts（枚举自动生成链路打通）
- 输出路径 `enums.js` → **`enums.ts`**（与 vue.ftl 的显式 `.ts` import 对应）。
- 内容从隐式全局赋值改为 **带 export 的 TS**，并补类型：
  ```ts
  export interface EnumItem { code: string; desc: string; }
  export const PASSENGER_TYPE: Record<string, EnumItem> = {ADULT: {code:"1", desc:"成人"}, ...};
  export const PASSENGER_TYPE_ARRAY: EnumItem[] = [{code:"1", desc:"成人"}, ...];
  ```
- 新增文件头注释（声明自动生成、勿手工改）+ `writeJs` 改名 `writeTs`。
- 新增枚举只需在 `ENUM_CLASSES` 列表登记。
- **端到端已验证**：后端 `PassengerTypeEnum` 加 `SENIOR("4","老人")` → `mvn install member` → 跑 EnumGenerator →
  前端自动出现 `{code:"4", desc:"老人"}`（验证后已回滚）。
- **工作流**：改后端枚举后执行
  `mvn -o -pl member install -DskipTests` 然后运行 `EnumGenerator`（必须 install，否则读到旧 jar）。

## 前端枚举的最终方案（收敛）
- 单一来源：`web/src/assets/js/enums.ts`，由 `EnumGenerator` 从后端 Java 枚举类反射生成。
- 之前手工维护 enums.ts 的方案作废；`EnumUtil` 已删（不需要再解析 Java 源码）。
- `vue.ftl` import 显式写 `@/assets/js/enums.ts`（防同名 .js 抢占解析）。

## 其它
- `vue.ftl` **已接入** `ServerGenerate`（`genVue(Domain, param)`），`readOnly` 参数已在 param 中（默认 false）。
- `generate/.../util/` 现在只有 DbUtil / Field / FreemarkerUtil 三个类。
- 验证枚举是否进生产产物：`Get-Content dist\assets\index-*.js -Encoding UTF8 -Raw` 后正则搜 `code:.{0,12}desc:`。
  **注意**：PowerShell 里直接写中文字符串字面量比较会因控制台编码失败，要用 `[char]0x6210 + [char]0x4EBA` 构造或用正则。
- 模板保留了 `defineOptions({ name: "${do_main}-view" })`（Passenger.vue 本身没有，但原模板有 name，保留不损失信息）。
- `git status` 会对内容未变的文件显示 "M"（stat 缓存/mtime 假阳性）。判断真改动要用
  `git hash-object --no-filters <file>` 对比 `git rev-parse HEAD:<file>`。
  **Windows 开了 autocrlf 时这两个哈希永远不同**（工作区 CRLF vs git 内 LF），此时以 `git diff --stat` 为空 为准。

## ⚠️ 反复踩的坑：验证模板时不要覆盖用户源文件
- 已两次因运行 `ServerGenerate` 覆盖 `web/src/views/Home/Passenger.vue`（62 增 / 37 删），均用 `git checkout --` 恢复。
- **正确流程**：生成前先备份，或临时把输出目录改到别处；验证完再决定保留哪份。
- **用户手写原版仍在 git 中**：`3febd02^`（含 `typeMap` 常量，11204 字节）。需要时用
  `git show "3febd02^:web/src/views/Home/Passenger.vue" > xxx.vue` 取回。
- 教训：之前那个 `Passenger.vue.orig.bak` 并不是手写原版，而是中间生成版本（无 typeMap、无 enums import）。
  **判断某份文件是不是手写原版，看是否有 `typeMap` 常量**，别只看文件名。

## PowerShell 里查 git 历史的两个坑（都会导致错误结论）
1. **`git rev-parse "rev:path"` 里的冒号被 PowerShell 当驱动器分隔符** → 返回空或 `/path` 字符串，
   导致"遍历所有 commit 找 blob"的脚本全部失配，误判"版本不在 git 历史中"。
   改用 `git show "rev:path" > file` 再检查内容。
2. **`git show ... | git hash-object --stdin` 不可靠** —— 输出经过 CRLF 转换，哈希与 `rev-parse` 结果不同。
   要判断内容时，直接导出到文件再搜索特征字符串（如 `typeMap`），不要比哈希。
